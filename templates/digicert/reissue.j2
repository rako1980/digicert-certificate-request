---
certificate:
  common_name: "{{ common_name }}"
  dns_names: {{ san_names | default(common_name,true) }}
  csr: "{{ csr }}"
  server_platform:
    id: {{ digicert_certificate_server_platform }}
  signature_hash: sha256
comments: Ansible Certificate request - Reissued
skip_approval: true
      

